import { BigIntInterceptorInterceptor } from './big-int-interceptor.interceptor';

describe('BigIntInterceptorInterceptor', () => {
  it('should be defined', () => {
    expect(new BigIntInterceptorInterceptor()).toBeDefined();
  });
});
